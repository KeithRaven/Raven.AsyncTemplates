@using Orchard.DisplayManagement.Descriptors
@using Orchard.Core.Shapes
@{
    Display.BindingAction = ViewBag.TemplateType;
}


@{
    var displayHelper = Display;
    displayHelper.BindingAction = BindingAction.Angular;

    var items = CoreShapes.Order(Model);
}

@DisplayChildren(items)